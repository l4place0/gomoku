## 1. 核心时序修复 (game.py)

- [x] 1.1 在 `game.py` 的 AI 普通回合入口处的 `if` 条件中，追加 `and not (len(history) == 3 and not asked_three)` 阻断条件，防止第 3 手落子后时序抢跑

## 2. 状态机与界面交互验证

- [x] 2.1 模拟人类执黑（先手）落子，验证在第 3 手时系统成功阻断 AI 落子并弹窗告知玩家 AI 的三手交换决策
- [x] 2.2 模拟人类执白（后手）落子，验证在第 3 手时系统正确弹窗询问玩家是否进行三手交换
