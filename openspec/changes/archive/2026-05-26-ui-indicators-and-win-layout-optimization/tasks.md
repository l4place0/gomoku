## 1. 基础设计与 UI 布局重构

- [x] 1.1 修复 `game.py` 底部胜负判定渲染，将 blit 基准横坐标由全屏 `WIDTH` 居中改为 `BOARD_AREA_W` 居中，实现与右下角“悔棋”按钮的完美避让。
- [x] 1.2 在 `game.py` 中实现通用的 `draw_blocking_overlay(title, subtitle, color)` 辅助遮罩渲染函数，用于绘制半透明图层与卡片。
- [x] 1.3 重构原有的常规思考蒙版逻辑，复用并调用 `draw_blocking_overlay` 渲染常规思考提示，保证视觉规范的统一性。

## 2. 状态机非人类动作环节视觉锁定与队列清空

- [x] 2.1 重构 `game.py` 的初始化流程，在 Pygame 窗口和字体初始化完成后，立即绘制启动载入进度画面并 flip，再顺序调用 `load_model_weights` 与 `load_kata_model` 载入底层。
- [x] 2.2 在三手交换逻辑分支中，AI 串行执行双侧评分计算前，引入黄色遮罩蒙版 `"AI 正在评估三手交换决策..."` 的渲染刷新，并在计算完成后调用 Pygame 事件清空，避免二次误触。
- [x] 2.3 在五手 N 打 AI 执黑筛选候选点前，引入红色遮罩蒙版 `"AI 正在选择五手N打候选点..."` 的渲染刷新，并在计算完成后调用 Pygame 事件清空，避免点击积压。
- [x] 2.4 在五手 N 打 AI 执白评估保留点前，引入蓝色遮罩蒙版 `"AI 正在评估保留点..."` 的渲染刷新，并在计算完成后清空点击事件。

## 3. 全局对弈流程校验与系统功能测试

- [x] 3.1 运行主程序，验证首次加载大模型与权重时，游戏显示木纹风格加载背景及正确的模型载入提示。
- [x] 3.2 模拟进入第 3 手三手交换评估期与第 4 手五手 N 打评估期，检查对应颜色的锁定卡片是否能准时弹出且无任何主线程无响应感。
- [x] 3.3 进行一局完整对局测试直至决出胜负，确认游戏结束后的胜利喜报以棋盘中央对称完美呈现在底部状态栏中，悔棋按钮处于可读可点击状态，并且二者互不干扰。
