# Tasks: Golden Data Curriculum

- [x] 1. `automl_cli.py create_parser()` 添加 `--golden-min-visits` / `--golden-surprise-threshold` / `--golden-max-positions` 参数
- [x] 2. 实现 `mine_golden_positions()` 函数：从 search_logs 挖掘高价值位置写入 curriculum/
- [x] 3. 晋升流程中调用 `mine_golden_positions()`
- [x] 4. `run_shuffle()` 将 curriculum/ 加入 shuffle 输入目录
- [x] 5. 语法检查验证
